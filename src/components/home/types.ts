export interface HomeCard {
  id: number;
  label: string;
  title: string;
  sub: string;
  tone: string;
}

export interface HomeTile {
  id: number;
  title: string;
  sub: string;
  tone: string;
}
